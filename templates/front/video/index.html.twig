{% extends "front/layout.html.twig" %}

{% block title %}Vidéos - {% endblock %}

{% block mainContent %}
    <div class="container">
        <div class="card">
            <div class="card-body">
                <div class="row">
                    {% for video in videos %}

                    {% else %}
                        Pas de vidéos.
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}